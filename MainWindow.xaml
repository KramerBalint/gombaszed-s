<Window x:Class="gombaszedés.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Gombaszedés — Sakkfigurák" Height="520" Width="800">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="500"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" BorderBrush="Black" BorderThickness="2" Padding="6" Background="White">
            <UniformGrid Name="BoardGrid" Rows="8" Columns="8" Width="460" Height="460"/>
        </Border>

        <Grid Grid.Column="1" Margin="12">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Button Name="NewGameButton" Content="Új játék" Width="260" Height="64" Margin="0,6" HorizontalAlignment="Center" Click="NewGameButton_Click"/>

            <TextBlock Name="StatusText" Grid.Row="1" Margin="0,12" FontSize="16" HorizontalAlignment="Center"/>

            <StackPanel Grid.Row="2" Margin="0,20" HorizontalAlignment="Center">
                <Image Name="ResultImage" Width="220" Height="220" Stretch="Uniform" />
            </StackPanel>
        </Grid>
    </Grid>
</Window>
